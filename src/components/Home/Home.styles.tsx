import {createUseStyles} from 'react-jss'

const styles = createUseStyles({
    root: {
        maxWidth: 600,
        borderRadius: 20,
        borderWidth: 10,
    },
    headline: {
        paddingLeft: 30,
    },
});

export default styles;